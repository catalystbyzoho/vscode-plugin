#$Id$
product_name="VSCODE_PLUGIN"
product_version="0.0.2"
debug=on
optimize=off
deprecation=on
pre_version=false

#########################################################################################################

targetfull_order=generate_files

generate_files_order=exec:build_vsix

######################################################################################
###################### COMMON BUILD VARIABLES ########################################
#######################################################################################

build_dir=${basedir}/VSCODE_PLUGIN
source_basedir=${basedir}/..
tp_pkg=${build_dir}/thirdparty_packages
tp_src=${build_dir}/thirdparty_sources
up_pkg=${build_dir}/utility_packages
pkg_dir=${build_dir}/pkg
output_dir=${build_dir}/output

#########################################################################################################

output_dirname=${output_dir}

#########################################################################################################

build_vsix_exec_dir=${basedir}/../
build_vsix_exec_command=sh
build_vsix_exec_args=${basedir}/extension_builder.sh ${pre_version}

#########################################################################################################
